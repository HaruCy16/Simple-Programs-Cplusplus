/*
 PSEUDO CODES

 START
 1. input num1 and num2 ops;
 2. IF add(+) 
      THEN num1 + num2
 3. ELSE IF sub(-) 
          THEN num1 - num2
 4. ELSE IF mul(*) THEN num1 * num2
 5. ELSE IF div(/) THEN num1 / num2
      IF remainder is equal to 0
        THEN display result
      ELSE display "Cannot be divided by Zero"
 6. ELSE display error message
 7. Display result
  8. END

  POSITIVE OR NEGATIVE
  1. START
  2. Input number
  3. IF number is greater than 0 
        THEN display "Number is positive"
  4. ELSE IF number is less than 0
        THEN display "Number is negative"
  5. ELSE display "Number is zero"
  6. END

  TRAFFIC LIGHT
  1. START
  2. input light
  3. IF light is equal "red"
        THEN stop
  4. ELSE IF light is equal "yellow"
        THEN slow down
  5. ELSE IF light is equal "green"
        THEN go
  6. ELSE display "Invalid light color"
  7. END

  EVEN OR ODD
  1. START
  2. Input number
  3. IF number%2 == 0
        THEN display "Number is even"
  4. ELSE IF number is odd
        THEN display "Number is odd"
  5. ELSE display "Invalid input"
  6. END
*/